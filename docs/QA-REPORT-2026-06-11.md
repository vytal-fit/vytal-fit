# QA Report — Backend Migration (Phase 1+2)

**Date:** 2026-06-11/12 · **Tier:** Standard · **Build:** `a776162` (CI fully green)
**Environment:** local dev servers (ports 3100/3400) against the live seeded Neon database, real Better Auth session as `jose@vytal.fit` (owner, CrossFit Aveiro).
**Evidence:** 83 screenshots + driver scripts in `/tmp/vytal-qa/` (phases 0–7c). Report synthesized from two QA agent passes (the agents completed all flows; both were cut by session limits before writing up).

## Health score: 8.5 / 10 — SHIP

All migrated sections work end-to-end against the real database with real auth. Deductions are for known
not-yet-backed sub-features (documented below), not for defects in what shipped.

## Per-section verdicts

| Section | Verdict | Evidence (screenshots) |
|---|---|---|
| Login/auth | ✅ SHIP — wrong password shows inline error; correct login → dashboard | 00, 01 |
| Members | ✅ SHIP — list, detail, edit→save→toast→persists across reload, revert clean | 10–15, 73 |
| Classes | ✅ SHIP — agenda (today's seeded classes), calendar week view, create→appears in agenda+calendar→cancel removes it | 20–28c, 74, 75 |
| Class types | ✅ SHIP — create + delete with confirm (delete needed a retry once — see P2-1) | 22, 29–29c |
| CRM | ✅ SHIP — pipeline renders, lead create, drag stage move, persists across reload | 30–34 |
| WODs | ✅ SHIP — list, builder creates draft, publish flips badge, persists | 40–44b |
| Staff | ✅ SHIP — roster, create coach, delete with confirm | 50–54 |
| Locations | ✅ SHIP — list, create, delete | 55–58 |
| Exercises | ✅ SHIP (read-only) — library + detail with PRs panel; first load needed a retry once (see P2-2) | 60, 77, 78 |
| Dashboard check-ins | ✅ SHIP — live tile (4 seeded today; bumped to 6 by manual check-ins, unique-member count correct) | 70, 72, 72b |
| Member manual check-in | ✅ SHIP — button records check-in, list + dashboard tile update, persists on reload | 71–71c |
| Settings: features | ✅ SHIP — toggles load from DB, save persists across reload | 80–82 |
| Settings: branding | ✅ SHIP — accent color persisted via orgSettings | 83 |
| Plans | ✅ SHIP — plans + subscriber/revenue tables computed from real subscriptions | 76, 76b |

## Findings

**P0 — none.** No data loss, no cross-tenant leakage observed, no broken core flow.

**P1 (should fix in next phase):**
1. **No `leads.delete`** — QA/demo leads can only be parked in a stage, never removed. (API gap; the e2e suite works around it.)
2. **Cache-only mutations look real but don't persist**: WOD edit modal (no `wods.update`), plan activate/deactivate/delete (no `plans.update/delete`), exercise create/edit (router intentionally read-only). Users will believe they saved. Either add the procedures or disable the controls until backed.
3. **Org-isolation UI re-check not performed this pass** (org-switch flow wasn't re-driven after the auth flip). Isolation is heavily enforced and tested at the API layer (396 unit tests incl. per-procedure cross-tenant cases), but a UI-level switch-org spot check should be added to the e2e suite.

**P2 (polish):**
1. Class-type delete required one retry (likely the in-use CONFLICT path or a stale cache; transient). Watch for recurrence.
2. Exercises library needed one retry on first load (cold tRPC round-trip without a loading gate on one path).
3. Dashboard onboarding checklist ("2/10 passos completos") and several KPI tiles (revenue, churn, occupancy) are still mock — fine for demo, label or wire next.
4. Settings → Website page remains on localStorage by design (publicSite schema mismatch documented in BACKEND_WIRING.md).

## Environment gotchas (for future QA — not product issues)
- Dev server on a non-3000 port **requires** `BETTER_AUTH_URL=http://localhost:<port>` or all logins 401 ("Email ou palavra-passe incorretos") — burned two QA runs (`debug-login-fail*.png`).
- First-hit route compiles on a cold dev server inflate timings; warm the server before timed assertions.

## What the next phase should fix first
1. Add `wods.update`, `plans.update/delete`, `leads.delete` procedures (closes the cache-only traps).
2. UI org-switch e2e test (isolation visible at the UI layer).
3. Wire or visibly badge the remaining mock dashboard KPIs.
4. gym_members.user_id linkage → relax athlete self-service gates (booking, own results/PRs).
